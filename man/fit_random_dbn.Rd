% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbn_random.R
\name{fit_random_dbn}
\alias{fit_random_dbn}
\title{fit_random_dbn}
\usage{
fit_random_dbn(
  generated_dbn,
  type = c("discrete", "continuous", "mixed"),
  max_variables_cardinality = 2,
  fixed_cardinality = FALSE,
  param.lower = 0.2,
  param.upper = 0.8,
  sd = 1
)
}
\arguments{
\item{generated_dbn}{a DBN class object}

\item{type}{one of \code{"discrete"}, \code{"continuous"}, or \code{"mixed"}.}

\item{max_variables_cardinality}{(discrete only) An integer specifying the maximum cardinality to be used for the nodes.
If \code{fixed_cardinality} is \code{TRUE}, this parameter sets the exact cardinality for all nodes. If \code{fixed_cardinality}
is \code{FALSE}, this parameter defines the upper limit of the range from which the cardinality is randomly selected.}

\item{fixed_cardinality}{(discrete only) A logical parameter (\code{TRUE} or \code{FALSE}). If \code{TRUE},
all nodes will have the same cardinality as specified by
\code{max_variables_cardinality}. If \code{FALSE}, the cardinality for each node is randomly
determined, with a minimum of 2 and a maximum defined by \code{max_variables_cardinality}.}

\item{param.lower}{(continuous only) lower bound of the uniform from which regression coefficients are sampled.}

\item{param.upper}{(continuous only) upper bound of the uniform from which regression coefficients are sampled.}

\item{sd}{(continuous only) residual standard deviation assigned to every node.}
}
\value{
a dbn.fit object
}
\description{
Fits random parameters to a DBN structure. Dispatches to the appropriate
subroutine based on \code{type}:
\itemize{
\item \code{"discrete"}: random multinomial CPTs sampled from a Dirichlet prior
(see \code{\link[=generate_dbn_nodes_distributions]{generate_dbn_nodes_distributions()}}).
\item \code{"continuous"}: random gaussian regression coefficients sampled uniformly
on \verb{[param.lower, param.upper]} with fixed residual sd.
\item \code{"mixed"}: not yet implemented.
}
}
\examples{
\dontrun{
fit_random_dbn(dbn, type = "discrete", max_variables_cardinality = 2, fixed_cardinality = TRUE)
fit_random_dbn(dbn, type = "continuous", param.lower = .2, param.upper = .8, sd = 1)
}
}
