% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbn_random.R
\name{random.dbn}
\alias{random.dbn}
\title{random.dbn}
\usage{
random.dbn(
  nodes_names,
  is_same = TRUE,
  prob_edge_intraslice = 2/(length(nodes_names) - 1),
  prob_edge_interslice = 2/(length(nodes_names) - 1),
  prob_edges_g0 = prob_edge_intraslice,
  markov_order = 1
)
}
\arguments{
\item{nodes_names}{a character vector of node names.}

\item{is_same}{TRUE if G_0 should mirror the intra-slice arcs of G_t (mapped to
\code{g_0_arcs = TRUE} in \code{\link[=random.structure.dbn]{random.structure.dbn()}}). FALSE samples G_0 independently
with edge probability \code{prob_edges_g0}. Defaults to TRUE.}

\item{prob_edge_intraslice}{probability of an intra-slice edge (X_t -> Y_t).
Defaults to \code{2 / (length(nodes_names) - 1)}, which targets ~2 expected
intra-slice parents per node in a sparse random graph.}

\item{prob_edge_interslice}{probability of an inter-slice edge (X_t-k -> Y_t).
Either a single number applied to every lag, or a length-\code{markov_order}
vector of per-lag probabilities. Defaults to \code{2 / (length(nodes_names) - 1)}.}

\item{prob_edges_g0}{probability of an arc in the prior network G_0; only used
when \code{is_same = FALSE}. Passed through to \code{\link[=random.structure.dbn]{random.structure.dbn()}} as
\code{g_0_prob}. Defaults to \code{prob_edge_intraslice}.}

\item{markov_order}{positive integer Markov order of the generated DBN.
Defaults to 1.}
}
\value{
a 'dbn' object.
}
\description{
Thin convenience wrapper around \code{\link[=random.structure.dbn]{random.structure.dbn()}}. Generates a random
DBN structure of arbitrary Markov order following the node ordering in
\code{nodes_names}.
}
\examples{
random.dbn(c("A","B","C","D","E"))                    # uses all defaults
random.dbn(c("A","B","C"), TRUE, 0.6, 0.5)
random.dbn(c("A","B","C","D"), is_same = FALSE,
           prob_edge_intraslice = 0.4, prob_edge_interslice = 0.4,
           prob_edges_g0 = 0.2)
# markov_order > 1 with per-lag interslice probabilities
random.dbn(c("A","B","C"), prob_edge_intraslice = 0.3,
           prob_edge_interslice = c(0.5, 0.2), markov_order = 2)
}
