% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbn_visualization.R
\name{plot.dbn}
\alias{plot.dbn}
\alias{plot.dbn.fit}
\title{Plot a Dynamic Bayesian Network}
\usage{
\method{plot}{dbn}(x, network = c("g_t", "g_0"), size = NULL, ...)

\method{plot}{dbn.fit}(x, network = c("g_t", "g_0"), size = NULL, ...)
}
\arguments{
\item{x}{an object of class \code{dbn} or \code{dbn.fit}.}

\item{network}{which network to draw: \code{"g_t"} (transition network, the
default) or \code{"g_0"} (prior network).}

\item{size}{horizontal width each row is spread across. Defaults to
\code{num_nodes * 1000}, where \code{num_nodes} is the number of variables in the
network.}

\item{...}{further arguments (currently unused).}
}
\value{
a \pkg{visNetwork} \code{htmlwidget}, drawn when printed.
}
\description{
Draws a DBN (\code{dbn}) or a fitted DBN (\code{dbn.fit}) as a time-slice grid using
\pkg{visNetwork}. The current slice sits on the bottom row and each lag is
stacked above it, up to the Markov order of the network:
\itemize{
\item transition network (\code{g_t}): row 0 holds the \code{X_t} nodes, row 1 the
\code{X_t-1} nodes, ..., up to \code{X_t-markov_order};
\item prior network (\code{g_0}): row 0 holds the \code{X_0} nodes, row 1 the \code{X_1}
nodes, ..., up to \code{X_(markov_order - 1)} when the DBN was built with
\code{extend_g0 = TRUE}.
}
Within every row the nodes are spread evenly across the same horizontal width
(\code{size}), so a row of 5 nodes and a row of 10 nodes both span the full width;
shrink or grow \code{size} to make the graph more compact or more spread out.

Because \code{plot.dbn} / \code{plot.dbn.fit} are S3 methods for the base
\code{\link[graphics]{plot}} generic, \code{plot(dbn)} works out of the box.
}
\examples{
\dontrun{
dbn <- random.structure.dbn(c("A", "B", "C"), .5, .5, markov_order = 2)
plot(dbn)                       # transition network
plot(dbn, network = "g_0")      # prior network
plot(dbn, size = 3000)          # more compact
}
}
