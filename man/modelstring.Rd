% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbn_helpers.R
\name{modelstring}
\alias{modelstring}
\alias{modelstring.default}
\alias{modelstring.dbn}
\title{Model string of a network}
\usage{
modelstring(x, ...)

\method{modelstring}{default}(x, ...)

\method{modelstring}{dbn}(dbn, ...)
}
\arguments{
\item{x}{a network object (\code{dbn}, \code{dbn.fit}, \code{bn} or \code{bn.fit}).}

\item{...}{further arguments passed to the method / \code{\link[bnlearn:modelstring]{bnlearn::modelstring()}}.}

\item{dbn}{an object of class 'dbn'}
}
\value{
for \code{dbn} / \code{dbn.fit} a named list (\code{g_0}, \code{g_t});
otherwise a single character string.

a named list with two character strings: \code{g_0} (prior network
model string) and \code{g_t} (transition network model string).
}
\description{
S3 generic returning the \pkg{bnlearn} model string of a network, dispatching
on the class of \code{x} just like \code{print}. For a \code{dbn} or
\code{dbn.fit} it returns a named list with the prior-network (\code{g_0}) and
transition-network (\code{g_t}) model strings, in the same format printed by
\code{\link[=summary.dbn]{summary.dbn()}}. For any other object (e.g. \code{bn}, \code{bn.fit}) it
delegates to \code{\link[bnlearn:modelstring]{bnlearn::modelstring()}}, returning a single string.
}
\examples{
\dontrun{
dbn <- random.structure.dbn(c("A","B","C"), .5, .5, 1)
ms  <- modelstring(dbn)
ms$g_0
ms$g_t
}
}
